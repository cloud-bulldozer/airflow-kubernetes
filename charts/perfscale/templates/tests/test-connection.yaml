apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "perfscale.fullname" . }}-test-connection"
  labels:
    {{- include "perfscale.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "perfscale.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

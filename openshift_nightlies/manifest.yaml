defaults:
  indexResults: true 

versions:
  stable:
    benchmarks:
      - uperf 
    aws:
      profiles: 
      - name: default
        benchmarks: 
          - http 
      - name: ovn
    azure: 
      profiles:
      - name: default
    gcp: 
      profiles:
      - name: default
  .next: 
    aws:
      profiles: 
      - name: default
        benchmarks: 
          - http 
      - name: ovn
    azure: 
      profiles:
      - name: default
    gcp: 
      profiles:
      - name: default 
  .future: 
    aws:
      profiles: 
      - name: default
        benchmarks: 
          - http 
      - name: ovn
    azure: 
      profiles:
      - name: default
    gcp: 
      profiles:
      - name: default